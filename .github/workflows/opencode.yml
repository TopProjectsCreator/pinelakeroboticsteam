name: opencode

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  opencode:
    if: |
      contains(github.event.comment.body, ' /oc') ||
      startsWith(github.event.comment.body, '/oc') ||
      contains(github.event.comment.body, ' /opencode') ||
      startsWith(github.event.comment.body, '/opencode') ||
      contains(github.event.comment.body, ' /code-canvas-bot') ||
      startsWith(github.event.comment.body, '/code-canvas-bot') ||
        contains(github.event.comment.body, ' /opencodebot') ||
      startsWith(github.event.comment.body, '/opencodebot') ||
          contains(github.event.comment.body, ' @opencode') ||
      startsWith(github.event.comment.body, '@opencode') ||
      contains(github.event.comment.body, ' /codecanvasbot') ||
      startsWith(github.event.comment.body, '/codecanvasbot')
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: read
      issues: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Run opencode
        uses: anomalyco/opencode/github@latest
        env:
          OPENCODE_API_KEY: ""
          OPENCODE_YOLO: "true"
          OPENCODE_DANGEROUSLY_SKIP_PERMISSIONS: "true"
        with:
          model: opencode/big-pickle
